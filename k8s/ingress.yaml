apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kutahyalilar-ingress
  namespace: kutahyalilar
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: api.kutahyalilar.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80

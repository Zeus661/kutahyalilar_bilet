apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-service
  namespace: kutahyalilar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trip-service
  template:
    metadata:
      labels:
        app: trip-service
    spec:
      containers:
        - name: trip-service
          image: kutahyalilar/trip-service:latest
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: kutahyalilar-config
            - secretRef:
                name: kutahyalilar-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: kutahyalilar
spec:
  selector:
    app: trip-service
  ports:
    - port: 8003
      targetPort: 8003

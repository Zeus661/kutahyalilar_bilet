apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: kutahyalilar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: kutahyalilar/notification-service:latest
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: kutahyalilar-config
            - secretRef:
                name: kutahyalilar-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: kutahyalilar
spec:
  selector:
    app: notification-service
  ports:
    - port: 8006
      targetPort: 8006

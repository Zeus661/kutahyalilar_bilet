apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: kutahyalilar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      containers:
        - name: payment-service
          image: kutahyalilar/payment-service:latest
          ports:
            - containerPort: 8005
          envFrom:
            - configMapRef:
                name: kutahyalilar-config
            - secretRef:
                name: kutahyalilar-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: kutahyalilar
spec:
  selector:
    app: payment-service
  ports:
    - port: 8005
      targetPort: 8005

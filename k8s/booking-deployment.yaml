apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: kutahyalilar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
        - name: booking-service
          image: kutahyalilar/booking-service:latest
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: kutahyalilar-config
            - secretRef:
                name: kutahyalilar-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: kutahyalilar
spec:
  selector:
    app: booking-service
  ports:
    - port: 8004
      targetPort: 8004
